services:
  app:
    build:
      context: .
    image: ai-code-reviewer:local
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
      RUN_MIGRATIONS: "true"
